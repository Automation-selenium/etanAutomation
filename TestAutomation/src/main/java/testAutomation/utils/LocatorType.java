package testAutomation.utils;

public class LocatorType {

    public enum Locator{
        ID,
        XPATH,
        CLASSNAME,
        CSS,
        LINKTEXT,
        NAME,
        PARTIALLINKTEXT,
        TAG
    }
}
